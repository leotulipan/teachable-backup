[project]
name = "teachable"
version = "0.1.0"
description = "Download course content from Teachable platform"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.11.11",
    "loguru>=0.7.3",
    "python-dotenv>=1.0.1",
    "requests>=2.32.3",
]
